import * as tb from 'react-icons/tb';
import * as fa from 'react-icons/fa';
console.log(Object.keys(tb).filter(k => k.toLowerCase().includes('face') || k.toLowerCase().includes('eye') || k.toLowerCase().includes('brain')));
console.log(Object.keys(fa).filter(k => k.toLowerCase().includes('face') || k.toLowerCase().includes('eye') || k.toLowerCase().includes('brain')));
